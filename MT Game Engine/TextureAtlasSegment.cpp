#include "TextureAtlasSegment.h"

//Constructor
TextureAtlasSegment::TextureAtlasSegment(const unsigned int OFFSET_S, const unsigned int OFFSET_T, const unsigned int WIDTH, const unsigned int HEIGHT) :
	OFFSET_S(OFFSET_S), OFFSET_T(OFFSET_T), WIDTH(WIDTH), HEIGHT(HEIGHT) {}